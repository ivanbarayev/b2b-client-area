import type { ThemeConfig } from "@/lib/theme-system/types"
export const themeConfig: ThemeConfig = {
  name: "Modern",
  version: "1.0.0",
  author: "Teamlify",
  description: "Clean top-navigation layout with full-width content area",
  supportsDarkMode: true,
  supportedLayouts: ["topnav"],
  previewColors: { primary: "#243d5c", accent: "#dbeafe", bg: "#f8fafc" },
}
